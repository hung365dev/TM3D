using UnityEngine;
using System.Collections;

namespace ManageMonster
{
	public class ManageMonsterCollar : MonoBehaviour {
		public UILabel collarDescription;
		public UILabel collarTitle;
		public UISlider collarCharge;
		public UIButton collarChargeButton;
		public UIButton collarChangeButton;
		// Use this for initialization
		void Start () {
		
		}
		
		// Update is called once per frame
		void Update () {
		
		}
	}
}